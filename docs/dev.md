## Local Development setup

`pip install -r requirements.txt`

` `

## Release checklist
  * Run `build.py` in `StardewSpeak\lib\speech-client\`. This will compress the python source files into a single executable, compile the C# code, and then finally create the a zip file of the release in `StardewSpeak\bin\Release.
